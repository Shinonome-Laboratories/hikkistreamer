services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - hikkistream_data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001

volumes:
  hikkistream_data:
